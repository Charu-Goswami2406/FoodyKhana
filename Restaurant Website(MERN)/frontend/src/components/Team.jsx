import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
          At FoodyKhana, our team is the heart of our success. Comprising passionate chefs, dedicated service professionals, and innovative tech experts, each member brings a unique flair and expertise to the table. United by a shared commitment to excellence and creativity, we work together to ensure every dining experience is exceptional. Get to know the people behind the scenes who make FoodyKhana a culinary destination like no other.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
